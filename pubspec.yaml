name: degust_et_des_couleurs
description: Appli de dégustation de restaurant.

publish_to: 'none'

version: 1.0.0+11

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^9.0.0
  google_fonts: ^5.1.0
  path_provider_foundation: ^2.2.3
  flutter_dotenv: ^5.1.0
  http: ^1.1.0
  flutter_typeahead: ^4.6.2
  jwt_decoder: ^2.0.1
  flutter_secure_storage: ^8.0.0
  intl: ^0.18.1
  loading_animation_widget: ^1.2.0
  firebase_core: ^2.20.0
  firebase_auth: ^4.12.0
  email_validator: ^2.1.16

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/images/logo.png
